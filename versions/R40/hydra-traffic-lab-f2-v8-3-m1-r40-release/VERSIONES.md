# Versiones Hydra Traffic Lab

## Versiones generales

- V1: fases basicas.
- V2: lazos, camara virtual y colas.
- V3: multi-cruce remoto/local.
- V4: diagnostico hardware y opticas.
- V5: mapa interactivo.
- V6: sincronizacion visible.
- V7: selector manual/automatico desde regulador.
- V8.1: mapa Leaflet/OpenStreetMap operativo.
- V8.2: editor de geometria, grupos luminosos, fases e incompatibilidades.
- V8.3: grupos operativos, maniobras, estados independientes y fases configurables.

## Rama moderna F2-V8.3-M1

Las revisiones `R` son cierres practicos de interfaz, navegacion, usuarios, proyectos y control sobre `F2-V8.3-M1`.

- F2-V8.3-M1-R20: menu lateral funcional por secciones; Inicio queda como pantalla completa.
- F2-V8.3-M1-R21: usuarios/roles, acceso local por PIN, menu de sesion y revision de botones sin accion.
- F2-V8.3-M1-R22: limpieza de cabecera, logo, textos y menu de usuario sobre mapa.
- F2-V8.3-M1-R23: proyectos/ciudades separados en roadmap; parte de la limpieza visual quedo fisicamente en esta carpeta.
- F2-V8.3-M1-R24: consolida R22/R23 e implementa proyectos/ciudades separados.
- F2-V8.3-M1-R25: permisos reales por rol; Configuracion, Sistema y Eventos quedan mejorados con ayuda contextual.
- F2-V8.3-M1-R26: usuarios personales, permisos por ciudad/proyecto y auditoria estructurada buscable.
- F2-V8.3-M1-R27: saneamiento R26 (encoding UTF-8, recategorizacion de 29 logs, verifyCredentials como punto de extension, auditoria persistente en localStorage propio con export JSON, UserFormModal sin prompts, filtros rol/ciudad/estado en panel Usuarios y PermissionsModal con checkboxes).
- F2-V8.3-M1-R28: Mapa de E/S DI/DO simulado (Advantech ADAM-6052 con 16 DI / 16 DO; lazos, pulsadores, selector, alarmas y opticas en tiempo real). Hito tecnico V8.3 en simulacion, listo para extender a E/S real en V9.
- F2-V8.3-M1-R29: limpieza visual transversal. Cabecera sin subtitulo redundante, descripciones siempre visibles sustituidas por boton (?) `HelpButton` con pistas + recordatorios de permisos en las 11 secciones, contadores duplicados retirados (Mapa del sistema, LeafletMapView, CorridorPanel, Corredores, Eventos). KPI canonico de cruces y tags identitarios se mantienen.
- F2-V8.3-M1-R30: borrar usuarios con jerarquia y bloqueos, cuenta unica por defecto `superadmin@hydra`, eliminado el ultimo texto descriptivo "Minimapa limpio…" del mapa operativo, `verifyCredentials` extendido para multi-factor (stub hasta V9) con `requiresMFA`, inactividad con avisos progresivos a 5/10/14 min + cierre a 15 min, `vite.config.js` inyecta build hash y fecha, export CSV de auditoria, dialogo "Reset/Conservar" al detectar nueva revision.
- F2-V8.3-M1-R31: layouts diferenciados (Mapa pantalla completa con HUD, Cruces con lista+tabs, Eventos como timeline con sidebar). Resto de secciones siguen patron R29 hasta validar.
- F2-V8.3-M1-R32: arreglos tras R31. Drag de marcadores funciona (hook de inactividad ya no causa re-render por movimiento). Mapa del sistema vuelve al layout de Inicio. Login con input manual de usuario. Cuenta default "Hydra System" / rol "Administrador del sistema".
- F2-V8.3-M1-R33: Centro de Control mas compacto (Acciones rapidas visibles), lateral derecho 360 px, tabs de Cruces con misma anchura, seccion Corredores renombrada a Coordinacion, tile + Añadir camara estilo Maniobras.
- F2-V8.3-M1-R34: refinos tras R33. Centro de Control mas compacto (110px), tooltips nativos en Acciones rapidas, cards corredor reducidas, Coordinacion con anchura uniforme, cards camara centradas y boton + en header restaurado.
- F2-V8.3-M1-R35: reparaciones tras R34. Camaras realmente centradas (track con columnas acotadas) y pills separados de los botones; cabecera "Centro de Control" en la misma posicion en todas las secciones (max-width/centrado movido a .hydra-shell + scrollbar-gutter); Coordinacion con panel de presentacion/estado siempre visible; crear/editar/login con PIN numerico y contrasena separados; entrega doble: fuente en versions/ + cliente-rXX.zip compilado sin fuente (sourcemap:false) como artefacto de la Action.
- F2-V8.3-M1-R36: QA + arreglo del crash critico de geometria. El selector de Geometria en "Control del cruce" guardaba un string en vez de objeto y reventaba el motor de senal (pantalla de error). Ahora usa onSetGeometry (objeto correcto via makeGeometry). Anadido normalizeCrossing en la carga: recupera estado corrupto persistido (geometry string) y garantiza arrivals/cameras/geometry validos, neutralizando de raiz el resto de crashes potenciales sin esparcir comprobaciones por el render. Barrido estatico de botones/acciones: name/offset con tipos correctos, sin handlers indefinidos.
- F2-V8.3-M1-R37: ancho completo. El tope de 1780px centrado en .hydra-shell que anadi en R35 (para fijar la cabecera) encogia todo el conjunto menu+contenido en pantallas anchas. Se quita ese tope: el layout usa todo el ancho de la ventana como en R39. La cabecera "Centro de Control" sigue fija en todas las secciones porque eso lo garantizan las otras dos reglas (.app.hydra-main sin recentrar + scrollbar-gutter:stable), no el tope.
- F2-V8.3-M1-R38: 4 mejoras de UI. (1) Menu "Corredores" roto desde R33 (boton "Corredores" vs seccion renombrada a "Coordinacion"): el boton pasa a "Coordinacion" y la seccion vuelve a mostrarse. (2) Camaras igual que R39: portado literal su bloque final de styles.css (~4908-4973) al final con !important; ademas CameraPanel deja en el track SOLO camaras (R38/R33 metia un tile "+ Anadir camara" dentro del track que quedaba mas pequeno que las camaras y descuadraba los :has() que cuentan tarjetas). El boton "+ Anadir camara" sigue en la cabecera y en el estado vacio. Resultado: 1 camara = 1 caja 260px centrada, 2 = dos, 3+ = tres, todas iguales, pantalla 150px, como R39. No se toca el header-fix R35.2/R37. (3) Eventos: .panel-header/.panel-tools con flex-wrap:wrap + bloque de texto encogible, asi los botones (Exportar JSON/CSV, Limpiar) ya no se montan sobre el texto. (4) Centro de Control: align-items:start y "Intervencion rapida"/"Resumen del sistema" apiladas en vertical a ancho completo, sin el hueco que provocaba align-items:stretch contra la tarjeta alta de Ajustes globales.
- F2-V8.3-M1-R38.1: reparacion de camaras tras ver que R38 no se parecia a R39 en las capturas. El puerto del commit anterior solo copio el primer bloque de camaras de R39 (4908-4973, cap 260px), pero R39 encadena TRES bloques (R36 5022-5121 + R39 5163-5260) y solo gana el ultimo: cada camara mide minmax(250px, 1fr) creciendo a ancho de columna, pantalla 190, gap 26, carousel padding 42. Se porta la cadena completa de R39 (3 bloques camera-only + media queries 1180/980/820). Cabecera del panel pasa a <h2>+<p> (texto de R39); empty-state pasa a la version rica de R39 (strong+span+Button). Se RESTAURA el tile "+ Anadir camara" dentro del carousel que tenia R36 y se habia eliminado: ahora R38 tiene tres formas de anadir camara (cabecera + empty-state + tile in-track) como R36, pero con el sizing visual de R39. QA exhaustivo del programa: todas las secciones, botones, sliders, modales y handlers verificados sin roturas (solo 2 detalles cosmeticos no funcionales en main.jsx:7185 y 4697 - se documentan, no se tocan).
- F2-V8.3-M1-R39: primera integracion de hardware real. Nuevo backend Node.js local (versions/R39/backend/: server.js Express+WebSocket, modbus-driver.js Modbus TCP propio con extension Flash ON/OFF de Waveshare, modbus-simulator.js, safety.js heartbeat watcher + shutdown handlers, conflict-matrix.js, audit-log.js JSONL persistente) que actua de puente entre el navegador y el modulo Waveshare Modbus POE ETH Relay 30CH en 192.168.1.44:502. Frontend: nueva seccion Hardware con HardwarePanel (status, configuracion, 30 toggles Q1-Q30, mapeo Q↔head+color por cruce, conflict matrix auto-derivada de geometry.conflicts, alarmas en directo via WS), hook useHardwareClient con reconexion automatica, integracion con el bucle de fases que emite cada segundo el estado deseado de Q1-Q30. Esquema de proyecto extendido con campo `hardware` (modbusEnabled, simulationMode, host/port/unitId, flashMs, mappings) sin romper proyectos antiguos. Seguridad multi-capa: watchdog interno del modulo via Flash mode 5s + watchdog backend 3s + conflict matrix server-side + reconciliacion all-off al conectar + shutdown handlers SIGINT/SIGTERM con all-off automatico. Snapshot cliente-r39-compilado/ con start.bat/start.sh que hacen npm install --omit=dev la primera vez. Build verificado, smoke test del backend en simulador OK (curl test-relay/set-relay/all-off/audit).
- F2-V8.3-M1-R40: pulido tras validacion R39 con hardware real. 6 fixes cosmeticos/UX: (1) safety.js no se arma hasta el primer tick (R39 disparaba safe-mode al arrancar antes incluso de conexion del frontend); (2) timeout safe-mode subido de 3s a 10s para evitar falsas alarmas por hiccups React + browser throttling; (3) backend emite cambio safe-mode tambien al SALIR para que el indicador rojo se quite cuando los ticks vuelven; (4) hardware-client.js resetea wsConnected/modbusConnected/inSafeMode/relays al perder el WS para no mostrar estado stale; (5) buildConflictPairs detecta tambien ped_green y green_arrow, no solo green vehicular, asi la matriz del panel Hardware muestra ya los conflictos peatonales; (6) main.jsx: el setInterval del bucle de fases ya no se recrea cada tick (deps = solo running, refs para leer valores frescos), eliminando los gaps que provocaba el throttling. Smoke test confirmado: 7s sin ticks = 0 alarmas safe-mode. R41 vendra con UX (auto-crear M al añadir grupo peatonal, renombrar "Apagar todo", limpieza mapeo Q→head huerfanos). R42 traera timings peatonales realistas (parpadeo + clearance).

## Notas

- No se renumeran carpetas antiguas.
- R24 corrige el descuadre entre roadmap y carpetas fisicas.
- Desde R24, el seguimiento correcto es: roadmap general para hitos grandes y roadmap R para revisiones practicas.
